@model IEnumerable<TeamManager.Models.PlayerRole>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.PlayerRoleDesc)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.PlayerRoleDesc)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.PlayerRoleID }) |
            @Html.ActionLink("Details", "Details", new { id=item.PlayerRoleID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.PlayerRoleID })
        </td>
    </tr>
}

</table>
