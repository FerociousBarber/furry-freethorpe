@model IEnumerable<TeamManager.Models.MatchEvent>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.PlayerID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.MatchID)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.PlayerID)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.MatchID)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.MatchEventID }) |
            @Html.ActionLink("Details", "Details", new { id=item.MatchEventID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.MatchEventID })
        </td>
    </tr>
}

</table>
